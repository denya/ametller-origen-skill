# dependencies
node_modules/

# local Ametller session / secrets — NEVER commit
.ametller/
session.json
storage_state.json
tickets/
*.sqlite
__pycache__/
*.local

# deterministic bundled runtime is committed for buildless plugin installs
dist/*
!dist/server.mjs

# packaged distributions
*.mcpb
*.zip
pack/
pack-skill/
ametller-unpacked/

# misc
.DS_Store
*.log
