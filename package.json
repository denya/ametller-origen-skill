{
  "name": "ametller-origen-skill",
  "version": "0.2.0",
  "description": "Agent skill, CLI, and MCP server for Ametller Origen catalog, cart, online orders, and Gmail receipts.",
  "type": "module",
  "license": "MIT",
  "author": "Dany and contributors",
  "homepage": "https://github.com/denya/ametller-origen-skill",
  "repository": {
    "type": "git",
    "url": "https://github.com/denya/ametller-origen-skill.git"
  },
  "keywords": [
    "ametller",
    "ametller-origen",
    "grocery",
    "groceries",
    "shopping",
    "supermarket",
    "cart",
    "reorder",
    "catalonia",
    "spain",
    "mcp",
    "claude",
    "claude-desktop"
  ],
  "scripts": {
    "cli": "node scripts/cli.mjs",
    "tickets:sync": "python3 scripts/sync_gmail_tickets.py",
    "test": "node --test test/*.test.mjs && python3 -m unittest discover -s test -p 'test_*.py'",
    "spike": "node scripts/spike.mjs",
    "login": "node scripts/login.mjs",
    "login:bundle": "npm run build && node scripts/login-mcp.mjs",
    "build": "esbuild src/server.mjs --bundle --platform=node --format=esm --target=node18 --loader:.md=text --banner:js=\"import { createRequire } from 'node:module'; import { fileURLToPath as bundleFileURLToPath } from 'node:url'; import { dirname as bundleDirname } from 'node:path'; const require = createRequire(import.meta.url); const __filename = bundleFileURLToPath(import.meta.url); const __dirname = bundleDirname(__filename);\" --outfile=dist/server.mjs --external:chromium-bidi/lib/cjs/bidiMapper/BidiMapper --external:chromium-bidi/lib/cjs/cdp/CdpConnection",
    "start": "node dist/server.mjs",
    "test:mcp": "npm run build && node scripts/test-mcp.mjs",
    "test:mcp:live": "npm run build && AMETLLER_LIVE_SMOKE=1 node scripts/test-mcp.mjs",
    "e2e:cart": "node scripts/e2e-cart.mjs",
    "e2e:readonly": "node scripts/e2e-readonly.mjs",
    "validate:plugin": "claude plugin validate . --strict",
    "validate:mcpb": "npx --yes @anthropic-ai/mcpb@2.1.2 validate manifest.json",
    "pack:mcpb": "npm run build && npx --yes @anthropic-ai/mcpb@2.1.2 pack . dist/ametller-origen.mcpb",
    "icon": "python3 scripts/make-icon.py"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "playwright-core": "^1.61.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
